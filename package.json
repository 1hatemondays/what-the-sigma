{
  "name": "dau-an-ho-chi-minh",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node --env-file-if-exists=.env.local server.js",
    "test": "node --test",
    "build": "node scripts/build.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
